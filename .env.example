# Required on Railway: set JWT_SECRET. PORT and DATABASE_URL are set by Railway when you add a service and PostgreSQL.

PORT=3000
NODE_ENV=production

# Required. Min 32 characters.
JWT_SECRET=your_secure_jwt_secret_here_minimum_32_characters_long

# Set by Railway when you add PostgreSQL. For local dev, use one of:
# DATABASE_URL=postgresql://user:password@host:port/database
# or
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=inventory
# PGUSER=postgres
# PGPASSWORD=postgres

# Optional
# PGPOOL_MAX=10
# ALLOWED_ORIGINS=https://your-app.vercel.app
